{
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter356 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter357 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter358 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter359 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter360 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter361 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter362 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter363 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter364 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter365 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter366 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter367 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter368 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter369 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter370 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter371 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter372 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter373 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter374 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter375 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter376 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter377 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter378 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter379 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter380 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter381 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter382 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter383 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter384 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter385 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter386 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter387 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter388 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter389 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter390 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter391 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter392 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter393 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter394 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter395 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter396 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter397 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter398 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter399 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter400 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter401 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter402 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter403 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter404 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter405 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter406 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter407 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter408 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter409 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter410 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter411 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter412 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter413 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter414 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter415 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter416 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter417 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter418 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter419 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter420 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter421 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter422 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter423 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter424 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter425 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter426 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter427 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter428 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter429 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter430 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter431 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter432 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter433 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter434 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter435 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter436 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter437 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter438 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter439 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter440 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter441 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter442 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter443 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter444 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter445 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter446 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter447 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter448 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter449 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter450 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter451 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter452 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter453 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter454 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter455 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter456 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter457 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter458 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter459 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter460 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter461 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter462 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter463 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter464 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter465 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter466 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter467 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter468 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter469 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter470 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter471 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter472 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter473 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter474 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter475 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter476 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter477 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter478 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter479 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter480 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter481 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter482 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter483 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter484 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter485 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter486 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter487 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter488 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter489 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter490 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter491 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter492 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter493 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter494 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter495 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter496 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter497 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter498 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter499 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
class Formatter500 {
public:
    static std::string formatNumber(double v,int decimals=2) { std::ostringstream ss;ss<<std::fixed<<std::setprecision(decimals)<<v;return ss.str(); }
    static std::string formatCurrency(double v,const std::string& curr="EUR") { std::ostringstream ss;ss<<std::fixed<<std::setprecision(2)<<v<<" "<<curr;return ss.str(); }
    static std::string formatPercentage(double v) { return formatNumber(v*100.0,1)+"%"; }
    static std::string formatDate(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d");return ss.str(); }
    static std::string formatDateTime(int64_t ms) { if(ms<=0)return"";auto t=std::chrono::system_clock::to_time_t(std::chrono::system_clock::from_time_t(ms/1000));std::ostringstream ss;ss<<std::put_time(std::localtime(&t),"%Y-%m-%d %H:%M:%S");return ss.str(); }
    static std::string formatDuration(int64_t seconds) { int64_t h=seconds/3600,m=(seconds%3600)/60,s=seconds%60;std::ostringstream ss;if(h>0)ss<<h<<"h ";if(m>0||h>0)ss<<m<<"m ";ss<<s<<"s";return ss.str(); }
    static std::string formatFileSize(int64_t bytes) { const char* u[]={"B","KB","MB","GB","TB"};int ui=0;double s=bytes;while(s>=1024&&ui<4){s/=1024;ui++;}std::ostringstream ss;ss<<std::fixed<<std::setprecision(1)<<s<<" "<<u[ui];return ss.str(); }
    static std::string formatIfNull(const std::string& v,const std::string& def="-") { return v.empty()?def:v; }
    static std::string formatBoolean(bool v) { return v?"Yes":"No"; }
    static std::string formatList(const std::vector<std::string>& items,const std::string& delim=", ") { std::ostringstream ss;for(size_t i=0;i<items.size();i++){if(i>0)ss<<delim;ss<<items[i];}return ss.str(); }
    static std::string formatJson(const json& j,bool pretty=true) { return pretty?j.dump(2):j.dump(); }
    static std::string formatTruncate(const std::string& s,int maxLen) { return s.size()>(size_t)maxLen?s.substr(0,maxLen-3)+"...":s; }
    static std::string formatPadLeft(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:std::string(len-s.size(),c)+s; }
    static std::string formatPadRight(const std::string& s,int len,char c=' ') { return s.size()>=(size_t)len?s:s+std::string(len-s.size(),c); }
};
}}}} // namespace