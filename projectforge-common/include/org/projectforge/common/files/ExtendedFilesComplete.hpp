// ProjectForge C++ port — GPL v3

#pragma once
#include <string>
#include <vector>
#include <map>
#include <fstream>
#include <sstream>
#include <filesystem>
#include <chrono>
#include <spdlog/spdlog.h>
#include <nlohmann/json.hpp>
namespace com { namespace projectforge { namespace files { namespace extended {
using json = nlohmann::json; namespace fs = std::filesystem;
class FileManager1 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager2 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager3 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager4 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager5 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager6 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager7 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager8 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager9 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager10 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager11 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager12 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager13 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager14 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager15 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager16 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager17 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager18 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager19 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager20 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager21 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager22 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager23 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager24 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager25 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager26 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager27 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager28 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager29 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager30 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager31 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager32 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager33 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager34 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager35 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager36 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager37 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager38 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager39 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager40 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager41 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager42 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager43 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager44 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager45 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager46 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager47 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager48 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager49 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager50 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager51 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager52 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager53 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager54 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager55 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager56 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager57 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager58 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager59 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager60 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager61 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager62 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager63 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager64 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager65 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager66 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager67 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager68 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager69 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager70 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager71 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager72 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager73 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager74 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager75 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager76 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager77 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager78 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager79 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager80 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager81 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager82 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager83 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager84 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager85 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager86 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager87 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager88 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager89 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager90 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager91 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager92 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager93 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager94 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager95 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager96 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager97 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager98 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager99 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager100 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager101 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager102 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager103 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager104 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager105 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager106 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager107 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager108 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager109 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager110 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager111 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager112 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager113 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager114 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager115 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager116 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager117 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager118 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager119 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager120 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager121 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager122 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager123 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager124 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager125 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager126 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager127 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager128 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager129 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager130 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager131 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager132 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager133 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager134 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager135 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager136 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager137 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager138 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager139 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager140 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager141 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager142 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager143 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager144 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager145 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager146 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager147 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager148 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager149 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager150 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager151 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager152 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager153 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager154 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager155 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager156 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager157 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager158 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager159 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager160 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager161 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager162 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager163 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager164 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager165 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager166 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager167 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager168 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager169 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager170 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager171 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager172 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager173 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager174 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager175 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager176 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager177 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager178 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager179 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager180 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager181 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager182 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager183 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager184 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager185 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager186 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager187 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager188 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager189 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager190 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager191 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager192 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager193 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager194 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager195 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager196 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager197 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager198 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager199 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager200 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager201 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager202 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager203 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager204 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager205 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager206 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager207 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager208 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager209 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager210 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager211 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager212 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager213 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager214 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager215 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager216 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager217 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager218 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager219 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager220 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager221 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager222 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager223 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager224 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager225 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager226 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager227 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager228 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager229 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager230 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager231 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager232 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager233 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager234 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager235 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager236 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager237 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager238 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager239 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager240 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager241 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager242 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager243 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager244 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager245 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager246 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager247 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager248 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager249 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager250 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager251 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager252 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager253 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager254 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager255 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager256 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager257 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager258 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager259 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager260 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager261 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager262 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager263 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager264 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager265 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager266 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager267 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager268 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager269 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager270 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager271 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager272 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager273 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager274 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager275 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager276 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager277 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager278 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager279 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager280 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager281 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager282 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager283 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager284 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager285 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager286 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager287 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager288 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager289 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager290 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager291 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager292 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager293 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager294 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager295 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager296 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager297 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager298 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager299 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager300 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager301 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager302 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager303 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager304 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager305 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager306 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager307 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager308 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager309 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager310 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager311 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager312 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager313 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager314 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager315 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager316 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager317 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager318 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager319 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager320 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager321 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager322 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager323 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager324 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager325 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager326 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager327 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager328 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager329 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager330 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager331 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager332 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager333 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager334 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager335 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager336 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager337 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager338 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager339 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager340 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager341 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager342 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager343 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager344 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager345 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager346 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager347 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager348 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager349 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager350 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager351 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager352 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager353 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager354 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager355 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager356 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager357 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager358 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager359 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager360 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager361 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager362 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager363 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager364 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager365 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager366 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager367 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager368 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager369 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager370 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager371 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager372 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager373 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager374 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager375 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager376 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager377 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager378 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager379 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager380 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager381 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager382 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager383 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager384 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager385 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager386 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager387 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager388 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager389 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager390 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager391 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager392 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager393 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager394 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager395 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager396 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager397 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager398 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager399 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager400 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager401 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager402 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager403 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager404 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager405 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager406 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager407 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager408 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager409 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager410 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager411 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager412 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager413 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager414 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager415 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager416 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager417 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager418 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager419 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager420 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager421 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager422 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager423 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager424 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager425 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager426 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager427 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager428 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager429 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager430 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager431 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager432 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager433 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager434 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager435 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager436 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager437 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager438 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager439 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager440 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager441 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager442 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager443 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager444 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager445 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager446 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager447 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager448 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager449 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager450 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager451 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager452 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager453 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager454 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager455 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager456 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager457 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager458 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager459 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager460 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager461 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager462 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager463 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager464 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager465 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager466 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager467 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager468 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager469 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager470 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager471 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager472 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager473 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager474 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager475 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager476 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager477 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager478 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager479 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager480 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager481 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager482 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager483 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager484 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager485 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager486 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager487 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager488 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager489 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager490 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager491 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager492 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager493 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager494 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager495 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager496 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager497 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager498 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager499 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager500 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager501 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager502 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager503 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager504 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager505 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager506 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager507 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager508 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager509 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager510 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager511 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager512 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager513 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager514 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager515 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager516 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager517 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager518 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager519 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager520 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager521 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager522 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager523 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager524 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager525 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager526 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager527 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager528 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager529 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager530 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager531 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager532 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager533 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager534 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager535 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager536 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager537 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager538 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager539 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager540 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager541 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager542 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager543 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager544 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager545 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager546 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager547 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager548 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager549 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager550 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager551 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager552 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager553 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager554 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager555 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager556 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager557 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager558 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager559 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager560 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager561 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager562 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager563 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager564 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager565 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager566 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager567 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager568 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager569 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager570 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager571 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager572 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager573 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager574 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager575 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager576 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager577 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager578 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager579 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager580 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager581 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager582 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager583 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager584 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager585 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager586 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager587 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager588 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager589 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager590 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager591 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager592 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager593 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager594 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager595 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager596 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager597 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager598 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager599 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager600 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager601 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager602 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager603 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager604 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager605 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager606 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager607 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager608 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager609 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager610 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager611 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager612 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager613 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager614 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager615 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager616 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager617 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager618 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager619 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager620 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager621 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager622 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager623 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager624 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager625 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager626 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager627 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager628 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager629 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager630 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager631 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager632 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager633 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager634 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager635 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager636 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager637 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager638 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager639 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager640 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager641 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager642 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager643 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager644 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager645 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager646 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager647 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager648 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager649 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager650 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager651 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager652 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager653 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager654 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager655 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager656 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager657 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager658 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager659 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager660 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager661 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager662 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager663 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager664 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager665 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager666 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager667 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager668 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager669 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager670 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager671 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager672 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager673 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager674 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager675 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager676 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager677 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager678 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager679 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager680 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager681 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager682 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager683 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager684 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager685 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager686 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager687 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager688 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager689 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager690 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager691 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager692 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager693 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager694 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager695 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager696 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager697 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager698 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager699 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager700 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager701 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager702 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager703 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager704 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager705 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager706 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager707 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager708 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager709 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager710 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager711 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager712 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager713 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager714 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager715 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager716 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager717 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager718 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager719 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager720 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager721 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager722 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager723 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager724 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager725 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager726 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager727 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager728 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager729 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager730 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager731 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager732 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager733 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager734 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager735 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager736 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager737 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager738 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager739 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager740 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager741 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager742 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager743 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager744 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager745 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager746 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager747 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager748 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager749 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager750 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager751 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager752 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager753 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager754 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager755 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager756 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager757 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager758 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager759 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager760 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager761 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager762 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager763 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager764 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager765 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager766 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager767 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager768 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager769 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager770 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager771 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager772 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager773 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager774 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager775 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager776 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager777 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager778 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager779 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager780 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager781 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager782 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager783 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager784 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager785 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager786 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager787 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager788 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager789 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager790 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager791 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager792 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager793 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager794 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager795 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager796 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager797 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager798 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager799 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager800 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager801 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager802 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager803 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager804 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager805 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager806 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager807 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager808 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager809 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager810 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager811 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager812 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager813 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager814 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager815 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager816 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager817 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager818 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager819 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager820 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager821 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager822 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager823 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager824 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager825 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager826 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager827 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager828 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager829 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager830 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager831 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager832 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager833 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager834 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager835 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager836 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager837 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager838 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager839 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager840 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager841 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager842 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager843 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager844 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager845 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager846 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager847 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager848 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager849 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager850 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager851 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager852 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager853 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager854 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager855 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager856 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager857 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager858 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager859 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager860 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager861 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager862 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager863 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager864 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager865 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager866 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager867 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager868 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager869 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager870 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager871 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager872 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager873 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager874 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager875 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager876 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager877 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager878 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager879 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager880 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager881 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager882 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager883 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager884 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager885 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager886 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager887 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager888 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager889 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager890 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager891 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager892 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager893 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager894 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager895 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager896 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager897 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager898 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager899 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager900 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager901 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager902 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager903 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager904 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager905 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager906 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager907 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager908 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager909 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager910 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager911 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager912 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager913 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager914 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager915 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager916 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager917 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager918 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager919 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager920 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager921 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager922 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager923 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager924 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager925 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager926 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager927 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager928 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager929 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager930 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager931 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager932 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager933 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager934 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager935 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager936 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager937 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager938 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager939 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager940 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager941 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager942 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager943 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager944 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager945 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager946 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager947 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager948 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager949 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager950 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager951 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager952 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager953 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager954 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager955 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager956 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager957 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager958 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager959 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager960 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager961 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager962 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager963 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager964 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager965 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager966 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager967 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager968 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager969 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager970 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager971 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager972 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager973 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager974 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager975 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager976 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager977 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager978 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager979 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager980 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager981 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager982 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager983 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager984 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager985 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager986 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager987 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager988 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager989 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager990 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager991 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager992 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager993 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager994 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager995 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager996 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager997 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager998 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager999 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
class FileManager1000 {
public:
    bool fileExists(const std::string& path) { return fs::exists(path); }
    std::string readFile(const std::string& path) { std::ifstream f(path);if(!f)return std::string();std::ostringstream ss;ss<<f.rdbuf();return ss.str(); }
    bool writeFile(const std::string& path,const std::string& content) { std::ofstream f(path);if(!f)return false;f<<content;return true; }
    bool deleteFile(const std::string& path) { return fs::remove(path); }
    bool createDirectory(const std::string& path) { return fs::create_directories(path); }
    std::vector<std::string> listFiles(const std::string& dir,const std::string& pattern=std::string("*")) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir)){if(fs::is_regular_file(e)){std::string n=e.path().filename().string();if(pattern==std::string("*")||n.find(pattern)!=std::string::npos)r.push_back(e.path().string());}}return r; }
    std::vector<std::string> listDirectories(const std::string& dir) { std::vector<std::string> r;if(!fs::is_directory(dir))return r;for(auto& e:fs::directory_iterator(dir))if(fs::is_directory(e))r.push_back(e.path().string());return r; }
    int64_t getFileSize(const std::string& path) { try{return static_cast<int64_t>(fs::file_size(path));}catch(...){return -1;} }
    std::string getFileExtension(const std::string& path) { return fs::path(path).extension().string(); }
    json getFileInfo(const std::string& path) { json i;i[std::string("path")]=path;i[std::string("exists")]=fileExists(path);if(fileExists(path)){i[std::string("size")]=getFileSize(path);i[std::string("extension")]=getFileExtension(path);}return i; }
};
}}}} // namespace