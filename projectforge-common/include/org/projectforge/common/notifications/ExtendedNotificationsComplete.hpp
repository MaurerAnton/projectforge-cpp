// ProjectForge C++ port — GPL v3

#pragma once
#include <string>
#include <vector>
#include <chrono>
#include <spdlog/spdlog.h>
#include <nlohmann/json.hpp>
namespace com { namespace projectforge { namespace notifications { namespace extended {
using json = nlohmann::json;
class NotificationService1 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService2 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService3 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService4 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService5 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService6 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService7 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService8 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService9 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService10 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService11 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService12 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService13 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService14 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService15 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService16 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService17 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService18 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService19 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService20 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService21 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService22 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService23 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService24 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService25 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService26 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService27 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService28 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService29 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService30 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService31 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService32 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService33 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService34 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService35 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService36 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService37 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService38 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService39 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService40 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService41 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService42 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService43 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService44 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService45 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService46 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService47 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService48 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService49 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService50 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService51 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService52 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService53 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService54 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService55 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService56 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService57 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService58 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService59 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService60 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService61 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService62 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService63 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService64 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService65 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService66 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService67 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService68 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService69 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService70 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService71 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService72 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService73 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService74 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService75 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService76 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService77 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService78 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService79 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService80 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService81 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService82 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService83 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService84 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService85 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService86 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService87 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService88 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService89 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService90 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService91 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService92 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService93 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService94 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService95 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService96 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService97 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService98 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService99 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService100 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService101 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService102 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService103 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService104 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService105 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService106 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService107 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService108 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService109 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService110 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService111 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService112 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService113 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService114 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService115 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService116 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService117 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService118 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService119 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService120 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService121 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService122 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService123 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService124 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService125 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService126 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService127 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService128 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService129 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService130 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService131 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService132 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService133 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService134 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService135 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService136 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService137 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService138 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService139 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService140 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService141 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService142 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService143 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService144 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService145 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService146 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService147 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService148 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService149 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService150 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService151 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService152 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService153 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService154 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService155 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService156 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService157 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService158 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService159 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService160 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService161 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService162 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService163 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService164 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService165 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService166 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService167 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService168 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService169 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService170 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService171 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService172 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService173 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService174 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService175 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService176 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService177 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService178 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService179 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService180 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService181 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService182 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService183 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService184 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService185 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService186 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService187 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService188 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService189 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService190 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService191 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService192 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService193 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService194 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService195 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService196 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService197 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService198 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService199 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService200 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService201 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService202 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService203 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService204 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService205 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService206 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService207 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService208 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService209 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService210 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService211 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService212 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService213 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService214 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService215 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService216 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService217 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService218 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService219 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService220 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService221 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService222 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService223 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService224 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService225 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService226 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService227 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService228 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService229 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService230 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService231 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService232 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService233 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService234 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService235 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService236 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService237 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService238 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService239 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService240 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService241 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService242 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService243 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService244 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService245 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService246 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService247 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService248 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService249 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService250 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService251 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService252 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService253 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService254 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService255 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService256 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService257 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService258 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService259 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService260 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService261 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService262 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService263 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService264 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService265 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService266 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService267 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService268 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService269 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService270 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService271 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService272 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService273 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService274 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService275 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService276 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService277 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService278 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService279 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService280 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService281 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService282 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService283 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService284 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService285 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService286 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService287 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService288 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService289 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService290 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService291 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService292 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService293 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class Notification
{
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService617 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService618 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService619 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService620 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService621 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService622 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService623 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService624 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService625 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService626 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService627 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService628 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService629 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService630 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService631 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService632 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService633 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService634 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService635 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService636 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService637 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService638 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService639 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService640 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService641 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService642 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService643 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService644 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService645 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService646 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService647 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService648 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService649 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService650 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService651 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService652 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService653 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService654 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService655 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService656 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService657 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService658 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService659 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService660 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService661 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService662 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService663 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService664 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService665 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService666 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService667 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService668 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService669 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService670 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService671 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService672 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService673 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService674 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService675 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService676 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService677 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService678 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService679 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService680 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService681 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService682 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService683 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService684 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService685 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService686 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService687 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService688 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService689 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService690 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService691 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService692 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService693 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService694 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService695 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService696 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService697 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService698 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService699 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService700 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService701 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService702 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService703 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService704 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService705 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService706 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService707 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService708 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService709 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService710 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService711 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService712 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService713 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService714 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService715 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService716 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService717 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService718 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService719 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService720 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService721 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService722 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService723 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService724 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService725 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService726 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService727 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService728 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService729 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService730 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService731 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService732 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService733 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService734 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService735 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService736 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService737 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService738 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService739 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService740 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService741 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService742 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService743 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService744 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService745 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService746 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService747 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService748 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService749 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService750 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService751 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService752 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService753 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService754 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService755 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService756 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService757 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService758 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService759 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService760 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService761 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService762 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService763 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService764 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService765 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService766 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService767 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService768 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService769 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService770 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService771 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService772 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService773 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService774 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService775 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService776 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService777 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService778 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService779 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService780 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService781 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService782 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService783 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService784 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService785 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService786 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService787 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService788 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService789 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService790 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService791 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService792 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService793 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService794 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService795 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService796 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService797 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService798 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService799 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
class NotificationService800 {
public:
    struct Notification { int64_t id; std::string type,title,message,recipient,channel; int64_t createdAt,sentAt,readAt; bool read=false; json data; json toJson() const { json j;j[std::string("id")]=id;j[std::string("type")]=type;j[std::string("title")]=title;j[std::string("message")]=message;j[std::string("read")]=read;return j; } };
    Notification create(const std::string& t,const std::string& ti,const std::string& m,const std::string& r) { Notification n;n.id=++nextId_;n.type=t;n.title=ti;n.message=m;n.recipient=r;n.createdAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();notifs_.push_back(n);return n; }
    std::vector<Notification> getUnread(const std::string& r) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r&&!n.read)out.push_back(n);return out; }
    std::vector<Notification> getAll(const std::string& r,int limit=50) { std::vector<Notification> out;for(auto& n:notifs_)if(n.recipient==r){out.push_back(n);if(static_cast<int>(out.size())>=limit)break;}return out; }
    void markRead(int64_t id) { for(auto& n:notifs_)if(n.id==id){n.read=true;n.readAt=std::chrono::duration_cast<std::chrono::milliseconds>(std::chrono::system_clock::now().time_since_epoch()).count();break;} }
    int unreadCount(const std::string& r) { int c=0;for(auto& n:notifs_)if(n.recipient==r&&!n.read)c++;return c; }
    void clear() { notifs_.clear(); nextId_=0; }
private:
    std::vector<Notification> notifs_;
    int64_t nextId_=0;
};
}}}} // namespace