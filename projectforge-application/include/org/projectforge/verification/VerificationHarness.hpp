// Verification harness — tests all generated classes compile and run
#pragma once
#include <string>

namespace org::projectforge::verification {
    // Returns true if all verifications pass
    bool runAllVerifications();
}
